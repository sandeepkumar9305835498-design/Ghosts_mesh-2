.gradle/
build/
local.properties
.idea/
*.iml
captures/
.externalNativeBuild/
.cxx/

# Generated by the copyWebAssets Gradle task from the web app at the repo root.
# Never commit this — edit the web files at the repository root instead.
app/src/main/assets/www/

# Release signing secrets. NEVER commit these: anyone holding the upload key can
# publish as you. Keep the .jks/.keystore outside the repository when possible.
keystore.properties
*.jks
*.keystore
