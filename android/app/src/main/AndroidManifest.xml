<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- Online mode (PeerJS signalling) + OpenStreetMap tiles for the radar map -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <!-- QR pairing, in-app camera capture, video calls -->
    <uses-permission android:name="android.permission.CAMERA" />

    <!-- Voice notes and voice/video calls -->
    <uses-permission android:name="android.permission.RECORD_AUDIO" />
    <uses-permission android:name="android.permission.MODIFY_AUDIO_SETTINGS" />

    <!-- Radar map + "share live location" -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />

    <!-- navigator.vibrate() on new messages -->
    <uses-permission android:name="android.permission.VIBRATE" />

    <!-- Android 13+: needed before the system shows any notification for the app -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <!-- Native mesh (item 7): BLE proximity discovery ...
         Android 12+ splits Bluetooth into runtime permissions. neverForLocation
         tells the system BLE scanning is NOT used to derive location, so the
         app does not need the location permission just for nearby discovery.
         (ACCESS_FINE_LOCATION above is still used for the radar map / location
         sharing feature, and is required for BLE + Wi-Fi Direct on older
         releases where scanning was location-gated.) -->
    <uses-permission
        android:name="android.permission.BLUETOOTH_SCAN"
        android:usesPermissionFlags="neverForLocation" />
    <uses-permission android:name="android.permission.BLUETOOTH_ADVERTISE" />
    <uses-permission android:name="android.permission.BLUETOOTH_CONNECT" />

    <!-- Android 11 and below: legacy Bluetooth permissions -->
    <uses-permission android:name="android.permission.BLUETOOTH" android:maxSdkVersion="30" />
    <uses-permission android:name="android.permission.BLUETOOTH_ADMIN" android:maxSdkVersion="30" />

    <!-- Wi-Fi Direct (device-to-device links with no router/hotspot) -->
    <uses-permission
        android:name="android.permission.NEARBY_WIFI_DEVICES"
        android:usesPermissionFlags="neverForLocation" />
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />
    <uses-permission android:name="android.permission.CHANGE_WIFI_STATE" />

    <!-- Keeps nearby discovery alive while the app is backgrounded -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_CONNECTED_DEVICE" />

    <!-- Optional hardware, so the app also installs on devices without them -->
    <uses-feature android:name="android.hardware.camera" android:required="false" />
    <uses-feature android:name="android.hardware.camera.autofocus" android:required="false" />
    <uses-feature android:name="android.hardware.microphone" android:required="false" />
    <uses-feature android:name="android.hardware.location.gps" android:required="false" />
    <!-- Optional: the native mesh needs one of these, but the app is fully
         usable with neither (online + QR pairing keep working). -->
    <uses-feature android:name="android.hardware.bluetooth_le" android:required="false" />
    <uses-feature android:name="android.hardware.wifi.direct" android:required="false" />

    <application
        android:allowBackup="true"
        android:hardwareAccelerated="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.GhostMesh">

        <activity
            android:name=".MainActivity"
            android:configChanges="orientation|screenSize|screenLayout|smallestScreenSize|keyboardHidden|uiMode"
            android:enableOnBackInvokedCallback="true"
            android:exported="true"
            android:launchMode="singleTask"
            android:windowSoftInputMode="adjustResize">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Foreground service: BLE/Wi-Fi-Direct discovery + peer push while the
             app is in the background. connectedDevice matches the type passed to
             startForeground() in GhostMeshService. -->
        <service
            android:name=".GhostMeshService"
            android:exported="false"
            android:foregroundServiceType="connectedDevice" />
    </application>

</manifest>
